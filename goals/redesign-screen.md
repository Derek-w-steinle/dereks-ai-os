# Goal: redesign a screen with the frontend-design skill

Paste the block below after `/goal`, with auto mode on and a clean tree.

```text
Redesign the target screen using the frontend-design skill.
Done when:
- aesthetic-direction.md exists at repo root naming one specific aesthetic
- no Inter, Roboto, Arial, Open Sans, or system font appears in any stylesheet
  (grep returns 0 matches)
- the component renders without console errors
- accessibility lint reports 0 critical violations
- the test suite exits 0
Constraints: change only files under the target screen's folder and styles.
Report each verify command's output before declaring done.
Stop after 30 turns.
```
